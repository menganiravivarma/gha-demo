name: helloworld  #name of the workflow
on: workflow_dispatch # on what condition it has to trigger    
jobs:            # name of the job
  runs-on: ubuntu-latest      # it has to run on a VM
  name: hello
  steps:
    -name: helloworld:
     run: echo print "helloworls"
  
